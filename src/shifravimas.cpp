#include "shifravimas.h"
#include <iostream>
#include <fstream>
#include <sstream>

using namespace std;
string xorSifravimas(const string& duomenys, const string& raktas) {
    if(raktas.empty()) 
        return duomenys;
}


