cmake_minimum_required(VERSION 3.10) # 4.3 yra labai nauja versija, 3.10 dažniausiai užtenka

project(SlaptazodziuTvarkykle)

include_directories(include)

add_executable(programa 
    src/main.cpp 
    src/tvarkykle.cpp 
    src/irankiai.cpp 
    src/konfiguracija.cpp 
    src/duomenuValdymas.cpp
    src/shifravimas.cpp
    src/aes.c
)

# --- FAILŲ VALDYMAS ---

# Patikriname, ar failas egzistuoja "data" aplanke prieš kopijuodami
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/data/paskyros.txt")
    configure_file(data/paskyros.txt paskyros.txt COPYONLY)
else()
    message(STATUS "paskyros.txt nerastas - programa pati jį sukurs paleidimo metu.")
endif()

# Patikriname, ar egzistuoja config.cfg pagrindiniame aplanke
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/config.cfg")
    configure_file(config.cfg config.cfg COPYONLY)
else()
    message(STATUS "config.cfg nerastas - CMake praleidžia kopijavimą.")
endif()

include_directories(include)